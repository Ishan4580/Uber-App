server:
  port: 8081


spring:
  application:
    name: auth-service

    datasource:
        url: jdbc:postgresql://localhost:5432/auth_service_db
        username: postgres
        password: ishan123

    jpa:
       hibernate:
          ddl-auto: update
       show-sql: true

jwt:
  secret: mysecretkey
  expiry-ms: 3600000 # 1 hour

logging:
  level:
    root: INFO
    com.rideshare.auth_service: DEBUG